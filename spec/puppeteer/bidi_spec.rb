# frozen_string_literal: true

RSpec.describe Puppeteer::Bidi do
  it "has a version number" do
    expect(Puppeteer::Bidi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
